    // =============================================================================
    // XMD · 证据等级
    // =============================================================================
    // direct 表示当前 DOM 的直接媒体证据；strong 表示当前 Tweet 作用域内的结构化数据；
    // weak 仅作为兜底排序依据，不足以单独触发破坏性页面修改。
    XMD.evidence = Object.freeze({
        direct: 3,
        strong: 2,
        weak: 1,
        unknown: 0
    });
